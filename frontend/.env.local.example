# API Configuration
# Leave empty to use local Next.js stubs (for development without backend)
# Set to http://localhost:8000 to use FastAPI backend
NEXT_PUBLIC_API_URL=

# Admin token for FastAPI backend authentication (server-side only, NOT NEXT_PUBLIC_)
ADMIN_TOKEN=your-admin-token
